export const title = 'UDF Actors'

A class UDF becomes a Ray actor. Use it for anything with expensive setup: model weights, tokenizers, DB connections.

<Callout label="Tip">

Pass a `schema=` to `vlm` / `generate` to enforce structured JSON output and skip brittle parsing.

</Callout>
