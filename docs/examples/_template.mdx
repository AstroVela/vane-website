---
title: Example Template
---

Use this template when adding a new example page. Keep the page scoped to a reproducible workflow, make the input and output contracts explicit, and avoid claiming built-in integrations that are not available in the current source.

## A section

Describe the workflow, then include only code that can be copied into a small local script or adapted safely.
