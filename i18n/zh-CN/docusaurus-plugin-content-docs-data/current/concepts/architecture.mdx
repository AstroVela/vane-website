---
title: 架构
---

Vane Data 是一个多模态原生数据引擎。它提供兼容 DuckDB 的 SQL 与 Python 接口，在 relation 层支持 Python 和 AI 操作，并且可以在本地或通过 Ray 执行选定工作。

设计目标很简单：让 pipeline 保持表格形态，同时让 SQL、Python 库、模型推理和分布式执行处在同一个 workflow 中。

## 系统视图

一个 Vane Data pipeline 有四个用户可见层：

1. API 层：`import vane`、`vane.connect()`、兼容 DuckDB 的 SQL、relation 方法、UDF 和 AI Function。
2. relation pipeline：SQL 操作、投影、过滤、连接、UDF 阶段和写出都组合为 relation 步骤。
3. 执行 runner：本地执行用于开发和单节点任务；Ray runner 负责分发适合的扫描、写出和 UDF 工作。
4. UDF 运行时：Python 函数和可调用类通过任务或 actor backend 运行。

这种分层让 pipeline 可以先从本地开始；当数据量、模型吞吐量或资源放置需要时，再把选定工作迁移到 Ray。

## 公开用户接口

主要公开入口如下：

- `import vane`
- `vane.connect()`
- `vane.configure(...)`
- `vane.current_config()`
- `vane.env`
- 通过 `con.sql(...)` 和 `con.execute(...)` 使用兼容 DuckDB 的 SQL
- 兼容 DuckDB 的 relation API
- `rel.map_batches(...)`
- `rel.flat_map(...)`
- `rel.map(...)`
- `rel.embed_text(...)`
- `rel.classify_text(...)`
- `rel.prompt(...)`

为了兼容已有代码，DuckDB 风格的 import 仍然可用；但新的 Vane Data 项目应优先使用 `import vane`。

## runner 模式

Vane Data 有两种主要执行模式。

本地模式：

- 在本地进程和本机上运行。
- 适合作为开发、测试、轻量 SQL 和单节点 UDF 任务的起点。
- 支持 SQL、relation 操作和本地 UDF backend。

Ray 模式：

- 通过 `vane.configure(runner="ray")` 或 `VANE_RUNNER=ray` 选择。
- 使用 Ray 执行分布式扫描、分布式写出、分布式 UDF，以及基于 actor 的模型 worker。
- 要求每个 worker 都能访问 pipeline 所需的相同 Python 依赖、存储系统、模型文件和凭证。

如果当前进程已经初始化 Ray，runner 选择可能会与该环境相互影响。为了让生产脚本行为可预测，请在创建连接和运行 pipeline 前显式配置目标 runner。

## UDF 执行层

Python UDF 是 relation 操作。它们接收表格形态的输入，运行用户代码，并返回声明好的输出列。

根据可调用对象形态和资源需求选择 backend：

| Backend | 可调用对象形态 | 适用场景 |
| --- | --- | --- |
| `subprocess_task` | Python 函数 | 本地无状态 batch 转换 |
| `subprocess_actor` | 可调用类 | 本地模型或客户端复用 |
| `ray_task` | Python 函数 | 分布式无状态转换 |
| `ray_actor` | 可调用类 | 分布式模型复用或 GPU 支持的推理 |

任务 backend 适用于没有长期状态的函数。actor backend 适用于需要构造一次并跨 batch 复用的类。

## AI Function 层

AI Function 是基于同一 UDF 执行模型构建的 relation 方法：

- `embed_text` 生成 embedding 输出列。
- `classify_text` 生成标签输出列。
- `prompt` 从文本或多模态提示词生成响应输出列。

由 provider 支持的常见操作可以使用 AI Function。当你需要完全控制模型加载、batch 处理、输出 schema 或 provider 行为时，使用自定义 UDF。

## 配置模型

代码配置：

```python
import vane

vane.configure(runner="ray")
```

环境配置：

```bash
export VANE_RUNNER=ray
```

`vane.configure(...)` 会通过进程环境应用已注册的 `VANE_*` 设置。使用 `vane.current_config()` 或 `vane.env.as_dict()` 查看公开配置快照。

## worker 环境

Ray worker 执行任何工作时，都需要与 driver 保持一致的运行时上下文。实践中，请确认 worker 可以访问：

- UDF 和 AI provider 使用的 Python 模块。
- 对象存储凭证和端点设置。
- 模型文件或模型下载位置。
- 相关 `VANE_*`、`AWS_*`、`S3FS_*`、`DUCKDB_*`、`PYTHONPATH` 和 `RAY_ADDRESS` 环境变量值。

在创建连接或运行 pipeline 之前设置所需环境。

## 数据流动

常见数据路径：

- SQL 将本地或远程数据读取为兼容 DuckDB 的 relation。
- relation 将 Arrow batch 流式传入 Python UDF。
- `map_batches` 处理 Arrow 表，并返回声明好的输出列。当下游阶段仍然需要输入列时，也要从 UDF 中返回这些输入列。
- Ray 模式可以通过 Ray worker 物化分区。
- 写出会根据 runner 和目标，选择兼容 DuckDB 的 relation 写出路径或 Vane Data 的 relation 写出路径。

把过滤和列投影放在昂贵的 Python 或模型阶段之前。这样可以减少进入 Python 的数据量；在 Ray 模式下，也可以减少集群内的数据移动。
