---
title: convert_image
---

`convert_image` 在不缩放的前提下改变 `IMAGE` 的像素模式与位深。Alpha 会被保留、按完全不透明补齐，或在不合成的情况下丢弃。目标模式为常量时，保留输入类型中的固定尺寸；目标模式随行变化时，返回动态形状的 `IMAGE`。

## 签名

```python
vane.convert_image(image, mode) -> Expression
```

SQL：`convert_image(image, mode)`。Expression 方法：`expr.convert_image(mode)`。

## 参数

| 名称 | 类型 | 说明 | 默认值 |
| --- | --- | --- | --- |
| `image` | `IMAGE` 值或 `Expression` | 输入图像 | 必填 |
| `mode` | 模式名、`ImageMode`、`Expression` 或 `None` | 目标像素模式；`None` 返回 `NULL` | 必填 |

## 返回值与错误

返回 `IMAGE`。`NULL` 图像或模式返回 `NULL`。不支持的模式报错。

## 示例

```python
import vane

con = vane.connect()
con.sql("SELECT convert_image(decode_image_file(image_file('photo.png')), 'RGBA')").show()
```

## 来源与相关页面

- [图像函数](index.mdx)
- [实现：`vane/_image_operators.py`](https://github.com/AstroVela/vane/blob/main/vane/_image_operators.py)
