export const title = 'Configuration'

A single entrypoint sets the runner and global defaults. Call it before `connect()`.

| Variable | Effect |
|---|---|
| RAY_ADDRESS | Connect to an existing cluster |
| VANE_CACHE_DIR | Local scratch for spills |
| VANE_LOG_LEVEL | info / debug / warn |
