export const title = 'Troubleshooting'

## CUDA out of memory

Lower batch_size on the offending map_batches stage, or raise concurrency so fewer rows sit on each GPU at once.

## Low GPU utilization

Increase batch_size or read parallelism. Check that upstream SQL is not the bottleneck with VANE_LOG_LEVEL=debug.
